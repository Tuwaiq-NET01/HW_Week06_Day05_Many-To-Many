@{
    /*List<BranchModel> branches = (List<BranchModel>) ViewData["branches"];
    List<BranchModel> branchesBiggerThanThree = (List<BranchModel>) ViewData["branchesBiggerThanThree"];
    string branchy = (string) ViewData["Specific Id"];*/
    List<BranchModel> FoundBranches =   (List<BranchModel>) ViewData["SearchResults"] ; 
}

<!DOCTYPE html>

<html>
<head>

</head>
<body>
<div>
    <h1> Branches </h1>


    @*<table class="table table-dark">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">Area</th>
            <th scope="col">Address</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var branch in branches)
        {
            <tr>
                <th scope="row">@branch.id</th>
                <td>@branch.Name</td>
                <td>@branch.Area</td>
                <td>@branch.Address</td>

            </tr>
        }


        </tbody>
    </table>*@

    @*
    <h2> id Equals to 2 </h2> 
    
    <h2> @branchy </h2>
    
        <h2> id Bigger than 3  </h2> 
 <table class="table table-dark">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">Area</th>
            <th scope="col">Address</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var branch in branchesBiggerThanThree)
        {
            <tr>
                <th scope="row">@branch.id</th>
                <td>@branch.Name</td>
                <td>@branch.Area</td>
                <td>@branch.Address</td>

            </tr>
        }


        </tbody>
    </table>
    *@

    
           <h2> Found Result </h2> 
     <table class="table table-dark">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Name</th>
                <th scope="col">Area</th>
                <th scope="col">Address</th>
            </tr>
            </thead>
            <tbody>
            @foreach (var branch in FoundBranches)
            {
                <tr>
                    <th scope="row">@branch.id</th>
                    <td>@branch.Name</td>
                    <td>@branch.Area</td>
                    <td>@branch.Address</td>
    
                </tr>
            }
    
    
            </tbody>
        </table>
</div>
</body>
</html>